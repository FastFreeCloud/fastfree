{
  "name": "play-console-automation",
  "private": true,
  "type": "module",
  "description": "Playwright automation for Google Play Console bootstrap (create apps, invite SA, first AAB). Human logs in once; scripts reuse the session.",
  "scripts": {
    "setup": "node setup.mjs",
    "setup:app": "node setup.mjs --app"
  },
  "dependencies": {
    "playwright-core": "1.63.0"
  }
}
